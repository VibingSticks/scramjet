services:
  - type: web
    name: scramjet-proxy
    runtime: node
    plan: free
    rootDir: app
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: "22"
